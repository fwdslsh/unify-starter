{
  "name": "unify-complete-demo",
  "version": "1.0.0",
  "description": "A comprehensive demonstration of all Unify static site generator features and capabilities",
  "homepage": "https://unify-demo.example.com",
  "scripts": {
    "build": "unify build",
    "dev": "unify serve",
    "watch": "unify watch",
    "build:pretty": "unify build --pretty-urls",
    "build:minify": "unify build --minify",
    "build:production": "unify build --pretty-urls --minify --clean"
  },
  "devDependencies": {
    "@fwdslsh/unify": "^0.6.0"
  },
  "keywords": [
    "unify", 
    "static-site-generator", 
    "demo", 
    "starter-kit",
    "apache-ssi",
    "markdown",
    "bun",
    "templates",
    "components"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/fwdslsh/unify-starter"
  },
  "license": "MIT",
  "author": "Unify Team"
}
